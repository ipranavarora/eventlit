
import GridBoxes from '../Components/GeneralComponents/GridBoxes';


function HomeBody({ className }) {

    return (
        <div className={`${className} p-4 w-full`}>
            <GridBoxes />
            
        </div>

    );
}

export default HomeBody;